{
  "name": "general",
  "version": "1.0.0",
  "description": "## The Roster - Synopsis",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "seed": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "start": "concurrently \"cd backend && npm run start\" \"cd frontend && npm run start\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.2.2"
  }
}
